<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myweb.app.mapper.FoodsMapper">

  <!--/  获取所有food
  List<Foods> getAllFoodsList();
  //获取所有type
  List<Menu> getAllTypeList();-->
  <select id="getAllFoodsList" resultType="Foods">
    SELECT * from foods
  </select>
  <select id="getAllTypeList" resultType="Menu">
    select * from menu;
  </select>

</mapper>